/// <reference types="react" />
import { TPageProps } from '../util';
export declare function Configuration({ history, match: { params: { gameId } }, gameTemplate: { Create } }: TPageProps): JSX.Element;

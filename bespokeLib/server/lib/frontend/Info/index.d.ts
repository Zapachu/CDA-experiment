/// <reference types="react" />
import { RouteComponentProps } from 'react-router';
export declare function Info({ history, match: { params: { gameId } } }: RouteComponentProps<{
    gameId: string;
}>): JSX.Element;

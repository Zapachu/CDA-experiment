import {LogLevel} from './baseEnum'

export const config = {
    rootName: 'elfOtree',
    appPrefix: 'app',
    socketPath: '/elfOtree/socket.io',
    logLevel: LogLevel.log,
    minMoveInterval: 500
}
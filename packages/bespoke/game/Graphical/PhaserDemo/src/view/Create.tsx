import {Core} from 'bespoke-client-util'
import {FetchType} from '../config'
import {ICreateParams} from '../interface'

type TProps = Core.ICreateProps<ICreateParams, FetchType>

export function Create(props: TProps) {
    return null
}

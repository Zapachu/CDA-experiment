syntax = "proto3";

message CreateParams {
    string nextPhaseKey = 1;
    repeated string playUrls = 2;
}

message MoveParams {
    int32 stage = 1;
}

message PushParams {
}

message GameState {
}

message PlayerState {
    int32 currentStage = 1;
    int32 unlockedStage = 2;
}
